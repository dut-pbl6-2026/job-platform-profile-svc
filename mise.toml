[tools]
dotnet = "10.0.100"

[tasks.build]
description = "Build solution"
run = "dotnet build ProfileService.sln --warnaserror"

[tasks.test]
description = "Run tests"
run = "dotnet test ProfileService.sln"

[tasks.format]
description = "Check formatting"
run = "dotnet format --verify-no-changes ProfileService.sln"

[tasks.ef-check]
description = "Check EF pending model changes"
run = "dotnet ef migrations has-pending-model-changes --project src/Profile.Infrastructure --startup-project src/Profile.Api"

[tasks.run]
description = "Run API"
run = "dotnet run --project src/Profile.Api"

[tasks.pack-shared]
description = "Pack SharedKernel and copy to local-feed"
run = "dotnet pack ../job-platform-shared/src/SharedKernel/SharedKernel.csproj -c Release -o ../job-platform-shared/artifacts && cp ../job-platform-shared/artifacts/*.nupkg local-feed/"

[tasks.sync-env]
description = "Sync env from infra"
run = "../job-platform-infra/scripts/sync-env.sh dev"

[tasks.verify]
description = "Verify repos have .env"
run = "ls -1 ../job-platform-*/.env 2>/dev/null | wc -l; echo \" repos have .env (expected 14)\""
